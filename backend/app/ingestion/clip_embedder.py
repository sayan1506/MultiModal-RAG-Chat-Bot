# clip_embedder.py — NOT USED
#
# CLIP was replaced by Gemini Vision + gemini-embedding-001
# to avoid torch/GPU dependencies and stay on the free tier.
# Image embedding is now handled by gemini_embedder.embed_image().
